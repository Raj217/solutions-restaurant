import 'package:flutter/material.dart';

enum StorageValues { isDarkTheme }

// ======================== Duration ========================
const Duration baseDurationDelay = Duration(milliseconds: 500);
const Duration verySlowAnimDuration = Duration(seconds: 2);
const Duration slowAnimDuration = Duration(milliseconds: 1400);
const Duration mediumAnimDuration = Duration(milliseconds: 500);
const Duration fastAnimDuration = kThemeAnimationDuration;

// ======================== Size ========================
const double logoSizeSmall = 20;
const double logoSizeMedium = 25;
const double logoSizeLarge = 30;
